language: go

go:
  - "1.11"

env:
  - GO111MODULE="on"

install:
  - $GOPATH/bin/dep ensure -v

script:
  - go test ./...

after_success:
  - test -n "$TRAVIS_TAG" && curl -sL https://git.io/goreleaser | bash
